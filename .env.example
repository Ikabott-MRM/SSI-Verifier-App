# Local: copy to .env. EAS profiles in eas.json override for cloud builds.
# development / preview: non-prod API (see eas.json).
# production: set EXPO_PUBLIC_API_BASE_URL in eas.json to your final HTTPS host;
#   set EXPO_PUBLIC_API_KEY in Expo → Project → Environment variables (scope: production), not in git.

EXPO_PUBLIC_API_BASE_URL=https://identity-api.mangofield-2f4eea69.brazilsouth.azurecontainerapps.io
EXPO_PUBLIC_API_KEY=api-key-for-interacting-with-API-endpoints
